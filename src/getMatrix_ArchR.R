# Install and load packages
devtools::install_github("swaruplabUCI/ArchRtoSignac"
packages <- c("ArchRtoSignac", "ArchR","Seurat", "Signac","stringr") # required packages
loadinglibrary(packages)

# This script had been generated for ArchRtoSignac with more varaible Matrix not just peakMatrix and GeneScoreMatrix.
# But other matrixes and motifMatrix. 